/*
 * function: test type of statement using
 * structure: "using" "(" (left?="(")? qvar+=VariableName ("," qvar+=VariableName)* (right?=")")? ":" type=Type ")"
  block=BlockStatement
 * type: correct type
 * testpoint: for single qvar, type is qubittype or qubitarray, for multi qvar, type is tupletype, and contains qubittype and qubitarray
 */


operation syntax_using_test(): unit{

	using(q0:qubit){
		X(q0,180.0);
	}
	
	using(q1:qubit[]){
		
	}
	
	using((q,qx):(qubit,qubit[])){
		
	}
	
	using((q,qx,a):(qubit,qubit[],qubit)){
		
	}

}