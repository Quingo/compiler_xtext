/*
 * function: test type of expression toInt
 * structure: toInt "(" value=Expression ")"
 * type: correct type
 * testpoint: type is int and value type can be int, double
 */
 
 operation syntax_toint_test(): unit{
	int result;

	int a;
	double b;
	
	result = toInt(a);
	result = toInt(b);
}