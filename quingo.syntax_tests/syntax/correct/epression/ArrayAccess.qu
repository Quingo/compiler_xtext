/*
 * function: test type of expression arrayaccess
 * structure: var[Expression]*.length?
 * type: correct type
 * testpoint: type of arrayaccess, Expression type
 */
import config.json.*
import operations.*

operation syntax_arrayaccess_test(): unit{
	int[] result1;
	int result2;
	bool[] result3;
	
	int[] a;
	bool[][] b;
	
	result1 = a;
	
	result2 = a[0];
	
	result3 = b[1];
	
	using(q:qubit[3]){
		for(int i=0;i<3;i =i+1){
			init(q[i]);
			H(q[i]);
			
			result3[i] = measure(q[i]);
		}
	}
	
	result2 = b[1].length;
}