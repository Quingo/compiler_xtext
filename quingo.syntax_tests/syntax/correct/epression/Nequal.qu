/*
 * function: test type of expression Nequal
 * structure: Expression >= Expression, Expression > Expression, Expression <= Expression, Expression < Expression
 * type: correct type
 * testpoint: type of "Nequal" must be bool, left and right type should be the same type, double or int
 */


operation syntax_nequal_test(): bool{
	bool result;
	
	int c;
	int d;
	double e;
	double f;
	
	result = c >= d;
	
	result = e <= f;
	
	return result;
	
}