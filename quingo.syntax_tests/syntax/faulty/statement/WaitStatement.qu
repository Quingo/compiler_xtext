/*
 * function: test type of statement wait
 * structure: "wait" + "("lis+=[Variable] ("," lis+=[Variable])* "," delay=Expression")" ";"
 * type: faulty type
 * testpoint: lis is a list of qubits, type of delay is int
 */


operation syntax_wait_test1(): unit{
	
	int a;
	int[] b;
	wait(a,1);
	wait(b,2);
	
	using((q1,q2):(qubit,qubit[])){
		wait(q1,2.1);
		wait(q2,true);
	}
}

