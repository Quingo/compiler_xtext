/*
 * function: test type of expression arrayaccess
 * structure: var[Expression]*.length?
 * type: faulty type
 * testpoint: type of arrayaccess, Expression type
 */
import config.json.*
import operations.*

operation syntax_arrayaccess_test(): unit{
	int[] result1;
	int result2;
	bool[] result3;
	double result4;
	
	bool[] a;
	int[][] b;
	
	result1 = a;
	
	result2 = b[0];
	
	result3[1] = a;
	
	result4 = a.length;
	
	result2 = b[0][result4];
	
	result2 = b[result4][0];
	
	using(q:qubit[3]){
		for(int i=0;i<3;i =i+1){
			init(q[i]);
			H(q[i]);
			
			result1[i] = measure(q[i]);
		}
	}
	
}