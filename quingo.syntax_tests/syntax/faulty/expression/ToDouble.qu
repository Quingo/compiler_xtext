/*
 * function: test type of expression toDouble
 * structure: toDouble "(" value=Expression ")"
 * type: faulty type
 * testpoint: type is double and value type can be int, double
 */
 
 operation syntax_todouble_test(): unit{
	double result1;
	int result2;
	
	int a;
	bool b;
	int[] c;
	(int,int) d;
	
	result2 = toDouble(a);
	
	result1 = toDouble(b);
	
	result1 = toDouble(c);
	
	result1 = toDouble(d);
}