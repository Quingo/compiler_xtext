/*
 * function: test type of expression ExpTuple
 * structure: (Expression*)
 * type: faulty type
 * testpoint: type of ExpTuple is tuple
 */
 
 operation syntax_exptuple_test(): unit{
	(int,bool) a;
	
	(double[],bool) b;
	
	int[] c;
	
	a = (1,1, true);
	
	a = (true, false);
	
	b = (1.1, false);
	
	(int, (int, int)) d = (1, a);	
}